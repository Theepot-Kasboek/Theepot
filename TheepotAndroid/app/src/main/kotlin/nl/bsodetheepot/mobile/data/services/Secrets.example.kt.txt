package nl.bsodetheepot.mobile.data.services

/**
 * Kopieer dit bestand naar Secrets.kt (dat in .gitignore staat) en vul de
 * echte Supabase-waarden in — dezelfde als NEXT_PUBLIC_SUPABASE_URL /
 * NEXT_PUBLIC_SUPABASE_ANON_KEY uit de webapp.
 */
object Secrets {
    const val SUPABASE_URL = "https://JOUW-PROJECT.supabase.co"
    const val SUPABASE_ANON_KEY = "vul-hier-de-anon-key-in"
}
